#!/bin/bash

# Install necessary software (e.g., Python, pip, virtualenv)
sudo apt-get update -y
sudo apt-get ugrade -y
sudo apt-get install -y python3.10 
sudo apt-get install python3-pip -y
sudo apt install python3-venv -y
pip3 install flask


# Clone your Flask app repository (replace with your repository URL)
# git clone https://github.com/hamzaavvan/library-management-system.git
# cd library-management-system

# Configure your Flask app to use the PostgreSQL database
# Update your app's configuration with the PostgreSQL connection details

# Start your Flask app (replace with the actual command to start your app)
# export FLASK_APP=app.py
# export FLASk_ENV=development
# python -m flask run
